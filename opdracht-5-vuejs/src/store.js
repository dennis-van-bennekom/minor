import Vue from 'vue';
import Vuex from 'vuex';
import VueResource from 'vue-resource';

Vue.use(Vuex);
Vue.use(VueResource);

// Constants


// Initial state
const state = {};

// Actions
const actions = {};

// Mutations
const mutations = {};

// Combine all into store
export default new Vuex.Store({
  state,
  actions,
  mutations
});
